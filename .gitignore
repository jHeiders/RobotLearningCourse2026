__pycache__/
*.pyc
.venv/
*.egg-info/
.pytest_cache/
.ruff_cache/

# run outputs — configs are the registry, results are not committed
results/*
!results/.gitkeep
figures/*
!figures/.gitkeep
runs/
wandb/
*.zip
*.pt
*.mp4

reference/*

# local LaTeX builds — Overleaf compiles its own copy, and only tracked files
# are pushed back to it by `git subtree push`
report/build/